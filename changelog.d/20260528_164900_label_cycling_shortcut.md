### Added

- New keyboard shortcuts to cycle between labels of the current job
  (`]` for the next label, `[` for the previous label) and a shortcut
  to open the settings dialog directly on the Shortcuts tab (`F3`).
  All three shortcuts are user-configurable from the Shortcuts tab in
  the settings dialog.
  (<https://github.com/cvat-ai/cvat/pull/XXXX>)
